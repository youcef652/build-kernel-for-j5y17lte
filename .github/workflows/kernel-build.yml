name: Fixed Build Lineage 18.1 for J5 2017

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04 # نضمن وجود حزمة libncurses5 فوراً
    
    steps:
      # 1. تفريغ القرص يدوياً بالكامل لتفادي تجمد الـ apt وتأمين ~45 جيجابايت
      - name: Manual Free Disk Space
        run: |
          sudo rm -rf /usr/share/dotnet || true
          sudo rm -rf /usr/local/lib/android || true
          sudo rm -rf /opt/ghc || true
          sudo rm -rf /opt/hostedtoolcache/CodeQL || true
          sudo rm -rf /usr/local/share/boost || true
          docker image prune --all --force || true
          df -h

      # 2. تثبيت اعتماديات التجميع المحدثة
      - name: Install Build Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y bc bison build-essential ccache curl flex g++-multilib gcc-multilib git git-lfs gnupg gperf imagemagick lib32readline-dev lib32z1-dev libelf-dev liblz4-tool libsdl1.2-dev libssl-dev libxml2 libxml2-utils lzop pngcrush rsync schedtool squashfs-tools xsltproc zip zlib1g-dev python3 libncurses5

      # 3. تثبيت أداة Repo من السورس المباشر الآمن لجوجل
      - name: Install Google Repo Tool
        run: |
          mkdir -p ~/bin
          curl https://googlesource.com | base64 --decode > ~/bin/repo
          chmod a+x ~/bin/repo
          echo "$HOME/bin" >> $GITHUB_PATH

      # 4. تفعيل كاش GitHub Actions لحفظ ملفات البناء وتخطي حاجز الـ 6 ساعات
      - name: Cache ccache
        uses: actions/cache@v4
        with:
          path: ~/.ccache
          key: ccache-j5-manifest-${{ github.sha }}
          restore-keys: |
            ccache-j5-manifest-

      # 5. ضبط إعدادات الـ ccache بحجم 8 جيجا ليناسب المساحة المتاحة
      - name: Configure ccache
        run: |
          mkdir -p ~/.ccache
          ccache -M 8G
          ccache -o compression=true
          ccache -z

      # 6. تهيئة مستودع سورس LineageOS وإنشاء المانيفست المخصص لجهازك J5 2017
      - name: Initialize Repo and Sync with Device Manifest
        run: |
          mkdir android && cd android
          
          # تهيئة إعدادات الهوية الإجبارية
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git config --global color.ui false
          
          # تهيئة السورس الأساسي لـ LineageOS 18.1 بأقل عمق لترك مساحة
          repo init -u https://github.com/LineageOS/android.git -b lineage-18.1 --depth=1 --git-lfs --no-repo-verify
          
          # إنشاء مجلد الـ local_manifests السحري
          mkdir -p .repo/local_manifests
          
          # كتابة خريطة مستودعات هاتفك ومعالجك (Device, Common, Kernel, Vendor) لتجلبها الأداة ذاتياً تلقائياً
          cat <<EOF > .repo/local_manifests/j5y17lte.xml
          <?xml version="1.0" encoding="UTF-8"?>
          <manifest>
            <project name="SamsungGalaxyJ5-2017/android_device_samsung_j5y17lte" path="device/samsung/j5y17lte" remote="github" revision="lineage-18.1" depth="1" />
            <project name="SamsungGalaxyJ5-2017/android_device_samsung_exynos7870-common" path="device/samsung/exynos7870-common" remote="github" revision="lineage-18.1" depth="1" />
            <project name="SamsungGalaxyJ5-2017/FlareKernel_AOSP" path="kernel/samsung/exynos7870" remote="github" revision="lineage-18.1" depth="1" />
            <project name="SamsungGalaxyJ5-2017/android_vendor_samsung" path="vendor/samsung" remote="github" revision="lineage-18.1" depth="1" />
          </manifest>
          EOF
          
          # إطلاق المزامنة الشاملة لتقوم أداة repo بجلب كل شيء معاً بدون خطأ الـ git clone
          repo sync -c -j$(nproc --all) --force-sync --no-clone-bundle --no-tags --prune
          
          # تنظيف الملفات الزائدة فوراً لحماية قرص السيرفر من الامتلاء
          rm -rf .repo/project-objects .repo/projects/*/.git/objects/pack/tmp_*

      # 7. البناء الفعلي للروم بعد اكتمال جلب كافة الملفات بنجاح
      - name: Build ROM
        run: |
          cd android
          export USE_CCACHE=1
          export CCACHE_DIR=~/.ccache
          export _JAVA_OPTIONS="-Xmx4g"
          source build/envsetup.sh
          lunch lineage_j5y17lte-userdebug
          mka bacon -j$(nproc --all)

      # 8. رفع الملف النهائي (.zip) لتنزيله فوراً
      - name: Upload Artifacts
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: LineageOS-18-j5y17lte-ROM
          path: android/out/target/product/j5y17lte/*.zip
